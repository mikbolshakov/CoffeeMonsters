{
    "name": "hardhat-project",
    "scripts": {
        "format": "prettier --write \"**/*.{ts,js}\"",
        "update": "npx hardhat clean && npx hardhat compile"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^2.0.2",
        "hardhat": "^2.22.5",
        "prettier": "^3.2.5"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.2",
        "axios": "^1.7.2",
        "dotenv": "^16.4.5"
    }
}
